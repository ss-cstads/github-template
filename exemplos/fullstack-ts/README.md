# Exemplo Fullstack — Express + TypeScript + Angular + MySQL

A mesma aplicacao Task Manager, com backend em **Express.js + TypeScript + Prisma ORM**.

```
Internet → Frontend (Angular) → Backend (Express + TS + Prisma) → MySQL
```

**Login padrao:** usuario `admin`, senha `admin123`

O passo a passo de deploy e identico ao exemplo principal.
Consulte `exemplos/fullstack/README.md` para instrucoes detalhadas.
